Flask==2.0.1  # Older version with known vulnerabilities
PyJWT==1.7.1  # Older version with known vulnerabilities
requests==2.25.1  # Older version with known vulnerabilities
pymongo==3.12.0  # Older version with known vulnerabilities
python-dotenv==0.19.0
